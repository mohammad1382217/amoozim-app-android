*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
# Ignore the `build/` output directory of every module (root, app, core/*, feature/*),
# not just the root — no leading slash means it matches at any depth.
build/
/captures
.externalNativeBuild
.cxx
local.properties
*.apk
*.aab
*.keystore
!debug.keystore
